.DS_Store
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
lib/
pyvenv.cfg
CACHEDIR.TAG
.env
*.pdf
output/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.git_commit_msg.txt
example-pdfs/
test-project/

# MCP bundle build output
*.mcpb

# Eval run outputs (commit golden.jsonl only)
eval/*.json
eval/trace.jsonl

# Local planning notes + investigation assets — not part of the shipped
# product, kept out of history because of size/churn (binary crops, a
# growing plan that'll be superseded once the work lands)
docs/table-structure-repair/

# TLS certs/keys obtained via deploy/get-cert.sh — NEVER commit private keys.
certs/

# Bind-mounted server data dir (sqlite + figures + pdfs) when RAG_DATA_DIR
# points inside the repo — never commit the corpus.
rag-data/
